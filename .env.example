# Local example only; replace with your own PostgreSQL connection details.
DATABASE_URL="postgresql://formula:change-me@localhost:5432/formula_development?schema=public"
# Optional: separate disposable database; integration tests create/drop their own unique schema.
# TEST_DATABASE_URL="postgresql://formula:change-me@localhost:5432/formula_test"
